fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
litellm==1.93.0
openai==1.51.0
aiohttp==3.14.3
redis==5.0.8
python-multipart==0.0.31
azure-storage-blob==12.23.1

# Testing
pytest==9.0.3
pytest-asyncio==0.24.0
httpx==0.27.2
fakeredis==2.24.1

# Future phases (provisioned, not used in Phase 1 code):
# asyncpg / pgvector client — wired up in Phase 2 (RAG + Search)
asyncpg==0.29.0

# Eval harness (Phase 5c) — case files are YAML; see app/evals/.
PyYAML==6.0.2

# Monitoring (Phase 5d) — GET /metrics in Prometheus text-exposition
# format; see app/control/metrics.py.
prometheus-client==0.21.0
