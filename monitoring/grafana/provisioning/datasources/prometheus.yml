# Phase 5e — auto-provisioned so Grafana already has Prometheus wired up
# on first boot, no manual "Add data source" click-through needed.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
