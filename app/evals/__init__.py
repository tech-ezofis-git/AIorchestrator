"""The eval harness (Phase 5c) — see app/evals/runner.py and README.md's
"Eval harness (Phase 5c)" section. Not imported by app/main.py or anything
else the running service depends on: this package is a standalone
developer tool, invoked via `python -m app.evals.run`, never automatically.
"""
